name: master-cd

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build and push docker
        run: |
          docker build -t crustio/crust-docs:latest .
          docker login -u ${{ secrets.DOCKER_HUB_USER_NAME }} -p ${{ secrets.DOCKER_HUB_PASSWORD }}
          docker push crustio/crust-docs:latest
      - name: Upgrade docs
        uses: fifsky/ssh-action@master
        with:
          command: |
            docker pull crustio/crust-docs:latest
            cd /opt/compose
            docker-compose up -d crust-docs
          host: ${{ secrets.SSH_HOST }}
          user: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}